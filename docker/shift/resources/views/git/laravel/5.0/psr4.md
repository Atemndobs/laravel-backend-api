Adopt PSR-4 convention

Laravel 5 adopts the PSR-4 namespacing convention. This commit ensures
folders under the `App` namespace follow the same *TitleCase* naming
convention as the Laravel framework.
